# sys module
# - one of the system modules
# - provides system features

# import sys module
import sys

# print(dir(sys))

# get the platform name
# darwin = macOS
# linux  = linux
# win32  = windows
# print(f"platform = {sys.platform}")

# variable of int type
num = 200

# find the size of the variable
# print(f"size of num = {sys.getsizeof(num)} bytes")

# list of numbers
numbers = [10, 20, 30, 40, 50]
# print(f"size of numbers = {sys.getsizeof(numbers[0]) * len(numbers)} bytes")

