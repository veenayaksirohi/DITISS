# variable

# declare or define a variable named num
num = 200

# this is invalid initialization of a variable
# my_name

# print the value of num
# print(num)

# formatted string
print(f"num = {num}")

# since num is a variable, it can change its value
num = 300
print(f"num = {num}")

# since first_name is not yet defined, it cannot be referred
# print(f"first_name = {first_name}")

# since two statements are written on same line
# semi-colon (;) is required to terminate the first statement
# the last staement does not require ;
# salary = 15.50; can_vote = True

# if one line contains only one statement,
# semi-colon(;) is NOT required