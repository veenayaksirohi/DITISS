# PKI Lab — Digitally Signing Documents Using XCA

**Tool:** XCA (X Certificate and Key Management)  
**Lab Type:** CDAC DITISS — Security Lab  
**Exam Topic:** PKI Hierarchy · Digital Signatures · Certificate Chain

---

## Table of Contents

1. [Objective](#1-objective)
2. [Software Required](#2-software-required)
3. [Folder Structure](#3-folder-structure)
4. [Key PKI Terms](#4-key-pki-terms)
5. [Complete PKI Flow](#5-complete-pki-flow)
6. [Part 1 — Create Root CA](#6-part-1--create-root-ca)
7. [Part 2 — Create Sub CA](#7-part-2--create-sub-ca)
8. [Part 3 — Sign Sub CA Certificate (Root CA Signs)](#8-part-3--sign-sub-ca-certificate-root-ca-signs)
9. [Part 4 — Complete Sub CA Setup](#9-part-4--complete-sub-ca-setup)
10. [Part 5 — Create User Certificate (Tinku)](#10-part-5--create-user-certificate-tinku)
11. [Part 6 — Sign User Certificate (Sub CA Signs)](#11-part-6--sign-user-certificate-sub-ca-signs)
12. [Part 7 — Import Certificate Chain into Tinku DB](#12-part-7--import-certificate-chain-into-tinku-db)
13. [Part 8 — Export PFX (PKCS#12)](#13-part-8--export-pfx-pkcs12)
14. [Final Folder Structure](#14-final-folder-structure)
15. [Question 1 — Digitally Sign a Word Document](#15-question-1--digitally-sign-a-word-document)
16. [Question 2 — Digitally Sign a PDF Document](#16-question-2--digitally-sign-a-pdf-document)
17. [Viva / Exam Q&A](#17-viva--exam-qa)

---

## 1. Objective

Build a complete **3-tier PKI hierarchy** using XCA:

```
Root CA  →  Sub CA  →  User Certificate (Tinku)
```

Then use Tinku's certificate to:

- Digitally sign a **Microsoft Word** document
- Digitally sign a **PDF** document

---

## 2. Software Required

| Software                   | Purpose                                                     |
| -------------------------- | ----------------------------------------------------------- |
| XCA                        | Create and manage PKI (keys, certificates, CSRs, databases) |
| Microsoft Word             | Sign `.docx` documents                                      |
| Adobe Acrobat Reader / Pro | Sign `.pdf` documents                                       |

---

## 3. Folder Structure

Create these folders **before** starting the lab:

```
C:\
└── MyFiles\
    └── MyPKI\
        ├── 01-RootCA\
        ├── 02-SubCA\
        └── 03-Tinku\
```

> Each CA gets its own folder because each XCA instance creates its own `.xdb` database file.

---

## 4. Key PKI Terms

| Term                  | Meaning                                                                              |
| --------------------- | ------------------------------------------------------------------------------------ |
| **PKI**               | Public Key Infrastructure — the full system of CAs, certificates, and trust          |
| **CA**                | Certificate Authority — entity that issues and signs certificates                    |
| **Root CA**           | Top-level CA; self-signs its own certificate; highest trust anchor                   |
| **Sub CA**            | Intermediate CA; certified by Root CA; issues end-user certificates                  |
| **CSR**               | Certificate Signing Request — contains public key + identity; sent to CA for signing |
| **Private Key**       | Secret key used for signing; never shared                                            |
| **Public Key**        | Paired with private key; used by others to verify signatures                         |
| **Certificate**       | Binds an identity to a public key; signed by a CA                                    |
| **Certificate Chain** | Trust path: Root CA → Sub CA → User Certificate                                      |
| **Digital Signature** | Cryptographic proof of authenticity and integrity                                    |
| **XDB**               | XCA database file (stores keys, certs, CSRs)                                         |
| **PEM**               | Base64 text format for certificates and keys                                         |
| **CRT**               | Certificate file (binary or PEM)                                                     |
| **PFX / PKCS#12**     | Bundle: certificate + private key + full chain; used for signing in Word/PDF         |

---

## 5. Complete PKI Flow

```
┌─────────────────────────────────────┐
│              ROOT CA                │
│         (Self-Signed Cert)          │
│   Signs its own certificate         │
└──────────────────┬──────────────────┘
                   │  Signs Sub CA CSR
                   ▼
┌─────────────────────────────────────┐
│              SUB CA                 │
│     Certified by Root CA            │
│     Signs user certificates         │
└──────────────────┬──────────────────┘
                   │  Signs Tinku's CSR
                   ▼
┌─────────────────────────────────────┐
│         USER CERT (Tinku)           │
│     Certified by Sub CA             │
│     Key Usage: Digital Signature    │
└──────────────────┬──────────────────┘
                   │
         ┌─────────┴──────────┐
         ▼                    ▼
   Word Document          PDF Document
   (Digital Sign)         (Digital Sign)
```

---

## 6. Part 1 — Create Root CA

### Step 1 — Install XCA

Install XCA from the official source. On first launch, XCA prompts for a database location.

---

### Step 2 — Create Root CA Database

| Field             | Value                         |
| ----------------- | ----------------------------- |
| Folder            | `C:\MyFiles\MyPKI\01-RootCA\` |
| Database filename | `01-01-RootCA_DB.xdb`         |
| Password          | `root@123`                    |

> **Why a password?** It protects the private keys and certificates stored in the `.xdb` database file.

---

### Step 3 — Create Root CA Private Key

Navigate to: `XCA → Private Keys tab → New Key`

| Field     | Value        |
| --------- | ------------ |
| Name      | `RootCA-Key` |
| Algorithm | RSA          |
| Key Size  | 4096 bits    |

Click **Show Details** to verify key generation.

> The private key is the most critical component. It is used by Root CA to sign all Sub CA certificates and must never leave the Root CA database.

---

### Step 4 — Create Root CA Certificate

Navigate to: `XCA → Certificates tab → New Certificate`

Select template `Root CA` → click **Use this Template**.

#### Subject Tab

| Field            | Value         |
| ---------------- | ------------- |
| CN (Common Name) | `RootCA`      |
| O (Organization) | `ABC Pvt Ltd` |
| OU (Org Unit)    | `Security`    |
| C (Country)      | `IN`          |

#### Extensions Tab

Verify the following is set (auto-filled from template):

```
CA = TRUE
```

> This flag means this certificate can issue (sign) other certificates.

#### Key Usage Tab

Verify the following are selected (auto-filled from template):

```
✔ Certificate Sign
✔ CRL Sign
```

#### Signing

Select **Self Signed**.

> Root CA is the top of the chain — there is no higher authority to sign it. Issuer = Subject.

Click **OK** to create the Root CA certificate.

---

### Step 5 — Export Root CA Certificate (CRT)

```
Right-click RootCA certificate → Export → PEM / CRT format
```

| Output File     | Location                      |
| --------------- | ----------------------------- |
| `01-RootCA.crt` | `C:\MyFiles\MyPKI\01-RootCA\` |

> This `.crt` file will be imported into Sub CA and Tinku databases to build the trust chain.

---

### Step 6 — Export Root CA as PFX (PKCS#12)

Exporting Root CA as PFX bundles the **Root CA private key + self-signed certificate** into one encrypted file. This is useful for backup and for restoring the Root CA into another machine or XCA instance.

```
Right-click RootCA certificate → Export → PKCS#12 (.pfx)
```

| Field       | Value                         |
| ----------- | ----------------------------- |
| Output File | `01-RootCA.pfx`               |
| Location    | `C:\MyFiles\MyPKI\01-RootCA\` |
| Password    | `root@123`                    |

> **Why export Root CA as PFX?**
> The `.pfx` includes the **private key** (`RootCA-Key`). Without it, Root CA cannot sign Sub CA certificates on another machine. Keep this file extremely secure — anyone with this PFX and password can impersonate your Root CA.

Contents of `01-RootCA.pfx`:

| Component                | Purpose                          |
| ------------------------ | -------------------------------- |
| Root CA Certificate      | Self-signed identity             |
| RootCA-Key (Private Key) | Used to sign Sub CA certificates |

---

## 7. Part 2 — Create Sub CA

> Open a **new / separate XCA instance** — do not use the Root CA window.

### Step 1 — Create Sub CA Database

| Field             | Value                        |
| ----------------- | ---------------------------- |
| Folder            | `C:\MyFiles\MyPKI\02-SubCA\` |
| Database filename | `02-01-SubCA_DB.xdb`         |
| Password          | `subca@123`                  |

---

### Step 2 — Create Sub CA Private Key

Navigate to: `XCA → Private Keys tab → New Key`

| Field     | Value       |
| --------- | ----------- |
| Name      | `SubCA-Key` |
| Algorithm | RSA         |
| Key Size  | 4096 bits   |

---

### Step 3 — Create Sub CA CSR

Navigate to: `XCA → Certificates tab → New Certificate`

Select template **Sub CA**. At the bottom, choose **Create Signing Request (CSR)** instead of Self Signed.

#### Subject Tab

| Field | Value         |
| ----- | ------------- |
| CN    | `SubCA`       |
| O     | `ABC Pvt Ltd` |
| OU    | `Security`    |
| C     | `IN`          |

Click **OK** to generate the CSR.

---

### Step 4 — Export Sub CA CSR

```
XCA → Certificate Signing Requests tab → Right-click SubCA CSR → Export
```

| Output File       | Location                     |
| ----------------- | ---------------------------- |
| `02-02-SubCA.csr` | `C:\MyFiles\MyPKI\02-SubCA\` |

> A CSR contains the Sub CA's **public key** and **identity information**. It does **not** contain the private key. It is sent to Root CA for signing.

---

## 8. Part 3 — Sign Sub CA Certificate (Root CA Signs)

Switch back to the **Root CA XCA instance**.

### Step 1 — Import Sub CA CSR

```
XCA → Certificate Signing Requests tab → Import → 02-02-SubCA.csr
```

---

### Step 2 — Sign the CSR

```
Right-click the imported SubCA CSR → Sign
```

| Field               | Value    |
| ------------------- | -------- |
| Signing Certificate | `RootCA` |
| Template            | `Sub CA` |

Root CA uses `RootCA-Key` to sign Sub CA's public key, producing the signed Sub CA certificate.

---

### Step 3 — Export Signed Sub CA Certificate

```
Certificates tab (Root CA instance) → Right-click SubCA cert → Export → CRT format
```

| Output File       | Location                     |
| ----------------- | ---------------------------- |
| `02-03-SubCA.crt` | `C:\MyFiles\MyPKI\02-SubCA\` |

---

## 9. Part 4 — Complete Sub CA Setup

Switch back to the **Sub CA XCA instance**.

### Step 1 — Import the Signed Sub CA Certificate

```
XCA → Certificates tab → Import → 02-03-SubCA.crt
```

Sub CA now has:

- Its private key `SubCA-Key` (already in the database)
- Its certificate signed by Root CA

---

### Step 2 — Import Root CA Certificate into Sub CA DB

```
XCA → Certificates tab → Import → 01-RootCA.crt
```

> **Why?** This completes the certificate chain inside Sub CA's database. Without Root CA's certificate, chain verification fails.

Chain at this point:

```
Root CA
   └── Sub CA
```

---

### Step 3 — Export Sub CA as PFX (PKCS#12)

Now that Sub CA has both its **private key** (`SubCA-Key`) and its **signed certificate** in the same database, export the PFX.

```
Certificates tab → Right-click SubCA certificate → Export → PKCS#12 (.pfx)
```

| Field       | Value                        |
| ----------- | ---------------------------- |
| Output File | `02-SubCA.pfx`               |
| Location    | `C:\MyFiles\MyPKI\02-SubCA\` |
| Password    | `subca@123`                  |

> **Why export Sub CA as PFX?**
> The `.pfx` bundles the **SubCA-Key private key** with the certificate and chain. This allows Sub CA to be restored or moved to another machine and still sign user certificates.

Contents of `02-SubCA.pfx`:

| Component               | Purpose                          |
| ----------------------- | -------------------------------- |
| Sub CA Certificate      | Signed by Root CA                |
| SubCA-Key (Private Key) | Used to sign Tinku's certificate |
| Root CA Certificate     | Trust anchor (chain link)        |

---

## 10. Part 5 — Create User Certificate (Tinku)

Open a **third XCA instance**.

### Step 1 — Create Tinku Database

| Field             | Value                        |
| ----------------- | ---------------------------- |
| Folder            | `C:\MyFiles\MyPKI\03-Tinku\` |
| Database filename | `03-01-Tinku_DB.xdb`         |
| Password          | `tinku@123`                  |

---

### Step 2 — Create Tinku Private Key

Navigate to: `XCA → Private Keys tab → New Key`

| Field     | Value       |
| --------- | ----------- |
| Name      | `Tinku-Key` |
| Algorithm | RSA         |
| Key Size  | 2048 bits   |

> **Important:** Create this key inside the **Tinku DB** (`03-01-Tinku_DB.xdb`). The key and certificate must be in the same database for PFX export to work.

---

### Step 3 — Create Tinku CSR

Navigate to: `XCA → Certificates tab → New Certificate`

Use **Blank** template (not Root CA or Sub CA template). Choose **Create Signing Request (CSR)**.

#### Subject Tab

| Field | Value         |
| ----- | ------------- |
| CN    | `Tinku`       |
| O     | `ABC Pvt Ltd` |
| OU    | `Employees`   |
| C     | `IN`          |

#### Validity

```
1 Year (365 days)
```

#### Key Usage Tab

Select **only**:

```
✔ Digital Signature
```

Do **not** select:

```
✗ Certificate Sign
✗ CRL Sign
```

> Tinku is an end user, not a CA. Digital Signature allows signing documents, emails, and executables. Certificate Sign / CRL Sign are CA-only permissions.

Click **OK** to generate Tinku's CSR.

---

### Step 4 — Export Tinku CSR

```
Certificate Signing Requests tab → Right-click Tinku CSR → Export
```

| Output File       | Location                     |
| ----------------- | ---------------------------- |
| `03-02-Tinku.csr` | `C:\MyFiles\MyPKI\03-Tinku\` |

---

## 11. Part 6 — Sign User Certificate (Sub CA Signs)

Switch to the **Sub CA XCA instance**.

### Step 1 — Import Tinku CSR

```
XCA → Certificate Signing Requests tab → Import → 03-02-Tinku.csr
```

---

### Step 2 — Sign the CSR

```
Right-click Tinku CSR → Sign
```

| Field               | Value   |
| ------------------- | ------- |
| Signing Certificate | `SubCA` |

Sub CA uses `SubCA-Key` to sign Tinku's certificate.

---

### Step 3 — Export Signed Tinku Certificate

```
Certificates tab (Sub CA instance) → Right-click Tinku cert → Export → CRT format
```

| Output File       | Location                     |
| ----------------- | ---------------------------- |
| `03-03-Tinku.crt` | `C:\MyFiles\MyPKI\03-Tinku\` |

---

## 12. Part 7 — Import Certificate Chain into Tinku DB

Switch to the **Tinku XCA instance**.

### Import All Three Certificates

```
XCA → Certificates tab → Import (repeat for each file)
```

| File              | Role                               |
| ----------------- | ---------------------------------- |
| `03-03-Tinku.crt` | Tinku's own certificate            |
| `02-03-SubCA.crt` | Sub CA certificate (issuer)        |
| `01-RootCA.crt`   | Root CA certificate (trust anchor) |

Complete certificate chain inside Tinku's database:

```
Root CA
   └── Sub CA
          └── Tinku
```

> When Word or Adobe verifies the signature, they walk the chain `Tinku → Sub CA → Root CA`. If any link is missing, verification fails.

---

## 13. Part 8 — Export PFX (PKCS#12)

### Normal Export Steps

```
XCA → Certificates tab → Right-click Tinku cert → Export
```

Choose format: **PKCS#12 (.pfx)**

| Field       | Value                        |
| ----------- | ---------------------------- |
| Output File | `Tinku.pfx`                  |
| Location    | `C:\MyFiles\MyPKI\03-Tinku\` |
| Password    | `user123`                    |

Contents of `Tinku.pfx`:

| Component           | Purpose          |
| ------------------- | ---------------- |
| Tinku's Certificate | Identity         |
| Tinku's Private Key | Used for signing |
| Sub CA Certificate  | Chain link       |
| Root CA Certificate | Trust anchor     |

---

### Troubleshooting — PFX Option Not Appearing / Only .CRT in Dropdown

**Root cause:** XCA only shows the PKCS#12 (.pfx) export option when the selected certificate has a **matching private key in the same database**. If the key and certificate are in different databases, or if XCA cannot link them, it falls back to exporting only a `.crt` file.

**How to check:**

1. In the **Tinku XCA instance**, go to the **Private Keys** tab and confirm `Tinku-Key` is listed.
2. Go to the **Certificates** tab, right-click the `Tinku` certificate → **Show Details**.
3. Check the **Key** field — it must show `Tinku-Key`, not `no key`.

If it shows `no key`, the certificate is not linked to a key in this database and PFX export will not be offered.

---

#### Fix 1 — Recreate CSR in the Correct Database (Recommended)

This happens when the CSR was created in a different database from where you are trying to export.

1. Open the **Tinku XCA instance** (`03-01-Tinku_DB.xdb`).
2. Confirm `Tinku-Key` exists in the **Private Keys** tab.
3. Delete the existing Tinku CSR and Tinku certificate if present.
4. Create a **new CSR** for Tinku inside this same database (the key `Tinku-Key` will be auto-linked).
5. Export the new CSR as `03-02-Tinku.csr`.
6. Sign it again from the **Sub CA instance**.
7. Import the newly signed `03-03-Tinku.crt` back into this same Tinku DB.
8. Now right-click the Tinku certificate → Export → the **PKCS#12 (.pfx)** option should appear.

---

#### Fix 2 — Export CRT Instead and Change Extension (Will NOT Work for Signing)

Manually renaming a `.crt` file to `.pfx` does **not** produce a valid PFX. Word and Adobe require the private key bundled inside. Always use Fix 1.

---

#### Fix 3 — Merge Key and Certificate via Temporary Database

Use this only if Fix 1 is not possible.

1. Export `Tinku-Key` as PEM from whichever database holds it (`File → Export → PEM`).
2. Create a new temporary XCA database.
3. Import `Tinku-Key` (PEM) into the temporary DB.
4. Import `03-03-Tinku.crt`, `02-03-SubCA.crt`, and `01-RootCA.crt` into the temporary DB.
5. Right-click the Tinku certificate → Export → **PKCS#12 (.pfx)** → set password `user123`.
6. Save as `Tinku.pfx`.

---

## 14. Final Folder Structure

```
C:\MyFiles\MyPKI\
│
├── 01-RootCA\
│       01-01-RootCA_DB.xdb      ← XCA database
│       01-RootCA.crt            ← Root CA certificate (public, for chain building)
│       01-RootCA.pfx            ← Root CA bundle: cert + RootCA-Key (private key backup)
│
├── 02-SubCA\
│       02-01-SubCA_DB.xdb       ← XCA database
│       02-02-SubCA.csr          ← CSR sent to Root CA
│       02-03-SubCA.crt          ← Signed certificate from Root CA
│       02-SubCA.pfx             ← Sub CA bundle: cert + SubCA-Key + Root CA chain
│
└── 03-Tinku\
        03-01-Tinku_DB.xdb       ← XCA database
        03-02-Tinku.csr          ← CSR sent to Sub CA
        03-03-Tinku.crt          ← Signed certificate from Sub CA
        Tinku.pfx                ← Tinku bundle: cert + Tinku-Key + full chain (used for signing)
```

### PFX Summary

| PFX File        | Contains                               | Password    | Purpose                          |
| --------------- | -------------------------------------- | ----------- | -------------------------------- |
| `01-RootCA.pfx` | RootCA cert + RootCA-Key               | `root@123`  | Root CA backup / restore         |
| `02-SubCA.pfx`  | SubCA cert + SubCA-Key + Root CA chain | `subca@123` | Sub CA backup / restore          |
| `Tinku.pfx`     | Tinku cert + Tinku-Key + full chain    | `user123`   | Document signing in Word / Adobe |

---

## 15. Question 1 — Digitally Sign a Word Document

### Step 1 — Import Tinku.pfx into Windows Certificate Store

1. Double-click `C:\MyFiles\MyPKI\03-Tinku\Tinku.pfx`.
2. Certificate Import Wizard opens:
   - Store Location: `Current User`
   - Confirm file path
3. Enter PFX password: `user123`
4. Choose **Automatically select the certificate store**
5. Click **Finish** → you should see "Import successful"

---

### Step 2 — Create or Open a Word Document

Open Microsoft Word. Type some content, for example:

```
Hello,
My name is Tinku.
This document is digitally signed.
```

Save the document once before signing.

---

### Step 3 — Add a Digital Signature in Word

```
File → Info → Protect Document → Add a Digital Signature
```

In the signature dialog:

- Select certificate: **Tinku**
- Optionally add a reason / commitment type

Click **Sign**, then save.

---

### Step 4 — Verify the Signature

Close and reopen the document. Word should display:

```
✔ Digitally Signed
   Signed by: Tinku
   Certificate: Valid
```

> If the document is edited after signing, Word marks the signature as **Invalid** because the document hash no longer matches.

---

### What Word Verifies

| Property            | Explanation                                      |
| ------------------- | ------------------------------------------------ |
| **Integrity**       | Document has not been modified after signing     |
| **Authentication**  | Signed by Tinku (verified via certificate chain) |
| **Non-Repudiation** | Tinku cannot deny having signed the document     |

---

## 16. Question 2 — Digitally Sign a PDF Document

### Step 1 — Import PFX into Adobe Acrobat

```
Edit → Preferences → Signatures → Identities & Trusted Certificates → More…
```

1. Click **Digital IDs → Add ID…**
2. Choose **A file** → select `Tinku.pfx`
3. Enter password `user123` and finish

---

### Step 2 — Open PDF and Start Signing

```
Tools → Certificates → Digitally Sign
```

Draw a rectangle on the page where the signature should appear.

---

### Step 3 — Select Tinku Certificate and Sign

1. Select digital ID: **Tinku**
2. Optionally fill in reason, location, contact info
3. Enter PFX password if prompted: `user123`

Click **Sign** → save as `SignedDocument.pdf`.

---

### Step 4 — Verify Signature in Acrobat

Reopen `SignedDocument.pdf`. Acrobat should display:

```
Signature Valid
Signed By: Tinku
Certificate Chain: Trusted
```

---

### Certificate Chain Used During PDF Verification

```
PDF Document
      │
      │  Signed by Tinku (private key)
      ▼
Tinku Certificate
      │
      │  Issued by Sub CA
      ▼
Sub CA Certificate
      │
      │  Issued by Root CA
      ▼
Root CA Certificate
      │
      ▼
   TRUSTED ✔
```

---

## 17. Viva / Exam Q&A

**Q1. Why is Root CA self-signed?**

Root CA is the highest trust authority — there is no higher CA to sign it. So it signs its own certificate. The issuer and subject fields are identical.

---

**Q2. Why create a Sub CA instead of signing directly from Root CA?**

To keep the Root CA **offline and secure**. If the Root CA is compromised, the entire PKI collapses. The Sub CA handles day-to-day certificate issuance while Root CA remains protected.

---

**Q3. What is a CSR and what does it contain?**

A Certificate Signing Request contains:

- The applicant's **public key**
- **Identity information** (CN, O, OU, C)

It does **not** contain the private key. It is sent to a CA for signing.

---

**Q4. Why export a PFX file instead of just a `.crt`?**

A `.crt` contains only the certificate (public part). A `.pfx` (PKCS#12) also contains the **private key** and the **certificate chain**, encrypted with a password. Applications like Word and Adobe need the private key to create digital signatures.

---

**Q5. What happens if a document is modified after signing?**

The application recomputes a hash of the document and compares it with the hash stored inside the signature. If they differ, it reports: **Signature Invalid — Document has been tampered with.**

---

**Q6. Why import Root CA certificate into Sub CA and Tinku databases?**

To **complete the certificate chain**. When an application verifies Tinku's signature, it must trace the trust path: `Tinku → Sub CA → Root CA`. If any certificate in the chain is missing, verification fails.

---

**Q7. What is the difference between Key Usage: Digital Signature vs Certificate Sign?**

| Key Usage         | Who uses it                | Purpose                             |
| ----------------- | -------------------------- | ----------------------------------- |
| Digital Signature | End users (Tinku)          | Sign documents, emails, executables |
| Certificate Sign  | CAs only (Root CA, Sub CA) | Issue and sign other certificates   |

---

**Q8. Why does XCA show only `.crt` in the export dropdown instead of `.pfx`?**

XCA only offers PKCS#12 (.pfx) export when the selected certificate has a **matching private key in the same database**. If the private key and certificate are in different XCA databases, the key-certificate link is missing and XCA falls back to `.crt` export only. Fix this by recreating the CSR inside the correct database (the one that holds the private key), then re-signing and re-importing the certificate.

---

**Q9. What is the correct file naming sequence in this lab?**

| File                  | Stage                                                        |
| --------------------- | ------------------------------------------------------------ |
| `01-01-RootCA_DB.xdb` | Root CA database                                             |
| `01-RootCA.crt`       | Root CA certificate (public, for chain)                      |
| `01-RootCA.pfx`       | Root CA cert + private key bundle                            |
| `02-01-SubCA_DB.xdb`  | Sub CA database                                              |
| `02-02-SubCA.csr`     | Sub CA signing request                                       |
| `02-03-SubCA.crt`     | Sub CA signed certificate                                    |
| `02-SubCA.pfx`        | Sub CA cert + private key + Root CA chain bundle             |
| `03-01-Tinku_DB.xdb`  | Tinku database                                               |
| `03-02-Tinku.csr`     | Tinku signing request                                        |
| `03-03-Tinku.crt`     | Tinku signed certificate                                     |
| `Tinku.pfx`           | Tinku cert + private key + full chain (for document signing) |

imp lab exam

```
sudo which/locate appcahe2

mkdir -P /etc/apach2/ssl
cp cretificste / privste to /etc/apach2/ssl

chmod 600 -R /etc/apach2/ssl/\*

# enabled the ssl site

sudp tree /etc/apaceh2/sits-\*
a2emod ssl
a2emod default ssl

ssytemctl restart apache2

sudp tree /etc/apaceh2/sits-\*

# now configure ssl site

vi etc/appache/site-avalble/defau-ssl.conf
change
server admin webmaster@localhost
server sername www.shurahilab.local:443

      sslcertifacicatefile chabge to  etc /apceh2/ssl/ key.pem
      sslcertifacicatekey etc /apceh2/ssl/ .crt


      restart apache

dns should be working
open website in website in private wendow
```

# Lab 2 — Apache2 HTTPS with TLS Certificate (IP-Based)

**Tool:** XCA + Apache2 + OpenSSL
**Lab Type:** CDAC DITISS — Security Lab
**Topic:** PKI · TLS · Apache SSL Configuration · IP-Based Certificate

---

## Objective

Use the existing Root CA and Sub CA (from Lab 1) to issue a TLS server certificate for an Apache2 web server accessed by IP address (no DNS). Configure Apache2 to serve HTTPS on that IP.

---

## Pre-requisite

Root CA and Sub CA must already exist from Lab 1:

```
D:\PKI\MyPKI\
    ├── 01-RootCA\
    │       01-01-RootCA_DB.xdb
    │       01-RootCA.crt
    │       01-RootCA.pfx
    │
    └── 02-SubCA\
            02-01-SubCA_DB.xdb
            02-02-SubCA.pem       ← Sub CA CSR
            02-03-SubCA.crt       ← Signed Sub CA cert (needed for chain)
            02-SubCA.pfx
```

---

## Key Concept — Why IP Needs Special Handling

Browsers since 2017 **ignore the CN field** for hostname matching. The server IP must be in the **Subject Alternative Name (SAN)** extension as type `IP Address`, not as a DNS name.

```
WRONG:  SAN → DNS:192.168.80.100     ← browser rejects
CORRECT: SAN → IP Address:192.168.80.100  ← browser accepts
```

---

## Part 1 — Create Server Certificate in XCA (Windows)

### Step 1 — Create Server Database

Create a new folder and XCA database:

| Field    | Value                        |
| -------- | ---------------------------- |
| Folder   | `D:\PKI\MyPKI\04-IP-Server\` |
| Filename | `04-01-IPServer_DB.xdb`      |
| Password | `server@123`                 |

### Step 2 — Create Server Private Key

`Private Keys tab → New Key`

| Field     | Value          |
| --------- | -------------- |
| Name      | `IPServer-Key` |
| Algorithm | RSA            |
| Key Size  | 2048 bits      |

### Step 3 — Create Server CSR

`Certificates tab → New Certificate → Template: Blank → Create Signing Request`

**Subject tab:**

| Field | Value            |
| ----- | ---------------- |
| CN    | `192.168.80.100` |
| O     | `ABC Pvt Ltd`    |
| OU    | `IT`             |
| C     | `IN`             |

**Extensions tab — Add SAN (critical step):**

Click `Edit` next to `X509v3 Subject Alternative Name`

```
Type:  IP Address        ← must select IP Address, NOT DNS
Value: 192.168.80.100
→ Add → OK
```

The field should now show: `IP:192.168.80.100`

Also set:

- Basic Constraints → Type: `End Entity`
- Check `X509v3 Subject Key Identifier`
- Check `X509v3 Authority Key Identifier`

**Key Usage tab:**

```
✔ Digital Signature
✔ Key Encipherment
```

**Netscape / Advanced tab — Extended Key Usage:**

```
✔ TLS Web Server Authentication
```

Click OK → export CSR:

```
CSR tab → Right-click → Export
```

Save as `04-02-IPServer.csr` in `D:\PKI\MyPKI\04-IP-Server\`

### Step 4 — Sub CA Signs the Server CSR

Switch to **Sub CA XCA instance**.

```
CSR tab → Import → 04-02-IPServer.csr
Right-click → Sign
    Signing Certificate: SubCA
→ Go to Extensions tab → verify SAN shows: IP:192.168.80.100
→ OK
```

> If SAN is missing after signing, click Edit next to Subject Alternative Name and re-add `IP Address: 192.168.80.100` before clicking OK.

Export signed certificate:

```
Certificates tab → Right-click IPServer → Export → CRT
```

Save as `04-03-IPServer.crt` in `D:\PKI\MyPKI\04-IP-Server\`

### Step 5 — Verify SAN is Correct

On Linux, after copying the cert:

```bash
openssl x509 -in 04-03-IPServer.crt -text -noout | grep -A2 "Subject Alternative"
```

Expected output:

```
X509v3 Subject Alternative Name:
    IP Address:192.168.80.100     ← must say IP Address, not DNS
```

If it says `DNS:192.168.80.100` — recreate the certificate with the correct SAN type.

---

## Part 2 — Transfer Files to Linux Server

### Files Needed

| File                 | Location                        |
| -------------------- | ------------------------------- |
| `IPServer-Key.pem`   | Private key (exported from XCA) |
| `04-03-IPServer.crt` | Signed server certificate       |
| `02-03-SubCA.crt`    | Sub CA certificate              |
| `01-RootCA.crt`      | Root CA certificate             |

### Export Private Key from XCA

```
IPServer DB → Private Keys tab → Right-click IPServer-Key → Export → PEM
```

Save as `IPServer-Key.pem`

### SCP All Files to Linux

Run from Windows (PowerShell or CMD with OpenSSH):

```bash
scp "D:\PKI\MyPKI\04-IP-Server\IPServer-Key.pem" \
    "D:\PKI\MyPKI\04-IP-Server\04-03-IPServer.crt" \
    "D:\PKI\MyPKI\02-SubCA\02-03-SubCA.crt" \
    "D:\PKI\MyPKI\01-RootCA\01-RootCA.crt" \
    shuhari@192.168.80.100:/tmp/
```

Verify on Linux:

```bash
ls /tmp/*.crt /tmp/*.pem
```

---

## Part 3 — Configure Apache2 on Linux

### Step 1 — Copy Files to Apache SSL Directory

```bash
sudo mkdir -p /etc/apache2/ssl

sudo cp /tmp/IPServer-Key.pem   /etc/apache2/ssl/webserver.key
sudo cp /tmp/04-03-IPServer.crt /etc/apache2/ssl/webserver.crt

# Chain = SubCA first, then RootCA
cat /tmp/02-03-SubCA.crt /tmp/01-RootCA.crt | sudo tee /etc/apache2/ssl/chain.crt

# Secure the private key
sudo chmod 600 /etc/apache2/ssl/webserver.key
sudo chmod 600 -R /etc/apache2/ssl/
```

Verify files are in place:

```bash
ls -l /etc/apache2/ssl/
```

Expected:

```
chain.crt
webserver.crt
webserver.key
```

### Step 2 — Verify Certificate SAN

```bash
openssl x509 -in /etc/apache2/ssl/webserver.crt -text -noout | grep -A2 "Subject Alternative"
```

Must show:

```
IP Address:192.168.80.100
```

### Step 3 — Edit Apache SSL Config

```bash
sudo vi /etc/apache2/sites-available/default-ssl.conf
```

Find and update these lines:

```apache
SSLCertificateFile      /etc/apache2/ssl/webserver.crt
SSLCertificateKeyFile   /etc/apache2/ssl/webserver.key
SSLCertificateChainFile /etc/apache2/ssl/chain.crt
```

### Step 4 — Enable SSL and Restart Apache

```bash
sudo a2enmod ssl
sudo a2ensite default-ssl
sudo systemctl restart apache2
```

Check Apache status:

```bash
sudo systemctl status apache2
```

Should show: `active (running)`

Check which sites are enabled:

```bash
sudo ls /etc/apache2/sites-enabled/
```

---

## Part 4 — Install Root CA on Windows Client

Without this step, the browser will show "Not Trusted" even if the certificate is valid.

1. Copy `01-RootCA.crt` to the Windows client machine
2. Double-click the file → `Install Certificate`
3. Store Location: `Local Machine`
4. Place in: `Trusted Root Certification Authorities`
5. Click Finish → OK

---

## Part 5 — Test HTTPS

### Test from Linux (curl)

```bash
curl --cacert /tmp/01-RootCA.crt https://192.168.80.100
```

Expected: Apache default page HTML (no SSL error)

### Test from Browser (Windows)

Open browser in **private / incognito window**:

```
https://192.168.80.100
```

Expected: padlock icon with no warnings

Click the padlock to verify:

```
Issued to:  192.168.80.100
Issued by:  SubCA
Chain:      SubCA → RootCA ✔
```

---

## Troubleshooting

### Error: no alternative certificate subject name matches

```
curl: (60) SSL: no alternative certificate subject name matches target host name
```

**Cause:** SAN in the certificate is `DNS:192.168.80.100` instead of `IP Address:192.168.80.100`

**Fix:** Recreate the server certificate in XCA. In the SAN Edit dialog, select type **IP Address** (not DNS), enter the IP, click Add.

### Error: certificate file not found / wrong cert being served

**Fix:** Make sure files are actually copied to `/etc/apache2/ssl/` and restart Apache. Check with:

```bash
openssl s_client -connect 192.168.80.100:443 2>/dev/null | openssl x509 -noout -text | grep -A2 "Subject Alternative"
```

### Error: curl (77) error setting certificate file

```bash
# Install openssl if missing
sudo apt install openssl
```

---

## Final File Reference

### Actual Windows Folder Structure (D:\PKI> tree /F)

```
D:.
└───MyPKI
    ├───01-RootCA
    │       01-01-RootCA_DB.xdb       ← XCA database
    │       01-RootCA.crt             ← Root CA certificate (for chain / trust)
    │       01-RootCA.pfx             ← Root CA cert + private key backup
    │
    ├───02-SubCA
    │       02-01-SubCA_DB.xdb        ← XCA database
    │       02-02-SubCA.pem           ← Sub CA CSR (sent to Root CA for signing)
    │       02-03-SubCA.crt           ← Signed Sub CA certificate
    │       02-SubCA.pfx              ← Sub CA cert + key bundle
    │
    └───04-IP-Server
            04-01-IPServer_DB.xdb     ← XCA database
            04-03-IPServer.crt        ← Signed server certificate (SAN: IP Address)
            IPServer-Key.pem          ← Server private key (exported from XCA)
            IPServer.pfx              ← Server cert + key bundle (PKCS#12 backup)
            IPServer_crt_+_key.pem    ← Cert + key combined in one PEM file
            192.168.1.100.pem         ← Alternate cert export (PEM format)
```

### File Roles Explained

| File                     | Role                                 | Used for                                                     |
| ------------------------ | ------------------------------------ | ------------------------------------------------------------ |
| `01-RootCA.crt`          | Root CA public certificate           | Install on Windows client as trusted root; build `chain.crt` |
| `02-03-SubCA.crt`        | Sub CA public certificate            | Build `chain.crt` on Apache                                  |
| `04-03-IPServer.crt`     | Server certificate (SAN: IP Address) | `SSLCertificateFile` in Apache                               |
| `IPServer-Key.pem`       | Server private key                   | `SSLCertificateKeyFile` in Apache                            |
| `IPServer_crt_+_key.pem` | Cert + key combined                  | Alternative: use as single file for both SSL directives      |
| `IPServer.pfx`           | PKCS#12 bundle                       | Backup; import into browsers or other tools                  |
| `192.168.1.100.pem`      | Cert only in PEM format              | Alternate export; same as `.crt` but PEM named               |

> `IPServer_crt_+_key.pem` contains both the certificate and the private key in one file. If using this, point both `SSLCertificateFile` and `SSLCertificateKeyFile` to the same file — Apache will extract each from it automatically.

### SCP Command — Transfer to Linux

```bash
scp "D:\PKI\MyPKI\04-IP-Server\IPServer-Key.pem" "D:\PKI\MyPKI\04-IP-Server\04-03-IPServer.crt" "D:\PKI\MyPKI\02-SubCA\02-03-SubCA.crt" "D:\PKI\MyPKI\01-RootCA\01-RootCA.crt" shuhari@192.168.80.100:/tmp/
```

Verify on Linux:

```bash
ls /tmp/*.crt /tmp/*.pem
```

### Linux Apache2 Files

| File                                            | Purpose                                                           |
| ----------------------------------------------- | ----------------------------------------------------------------- |
| `/etc/apache2/ssl/webserver.crt`                | Copied from `04-03-IPServer.crt`                                  |
| `/etc/apache2/ssl/webserver.key`                | Copied from `IPServer-Key.pem`                                    |
| `/etc/apache2/ssl/chain.crt`                    | SubCA + RootCA concatenated (`cat 02-03-SubCA.crt 01-RootCA.crt`) |
| `/etc/apache2/sites-available/default-ssl.conf` | Apache SSL virtual host config                                    |

---

## Viva Q&A

**Q1. Why must the IP be in the SAN and not just the CN?**
Browsers ignore the CN field for hostname/IP matching since ~2017. The IP must be in the SAN extension as type `IP Address`. If it is set as `DNS` type, browsers reject it even though the value looks correct.

**Q2. What is the chain.crt file and why is it needed?**
It contains the Sub CA and Root CA certificates concatenated together. Apache sends this chain to the browser during the TLS handshake so the browser can verify the full trust path: Server → Sub CA → Root CA.

**Q3. Why is the order SubCA first, then RootCA in chain.crt?**
The chain must go from closest to the server outward to the trust anchor. Browser walks: server cert → Sub CA → Root CA. Reverse order causes verification failure.

**Q4. What happens if Root CA is not installed on the client machine?**
The browser cannot trust the chain — it will show a certificate warning (NET::ERR_CERT_AUTHORITY_INVALID). The connection still works with `curl -k` (insecure) but not with a normal browser visit.

**Q5. What key usage extensions does a TLS server certificate need?**

| Extension          | Value                               |
| ------------------ | ----------------------------------- |
| Key Usage          | Digital Signature, Key Encipherment |
| Extended Key Usage | TLS Web Server Authentication       |
| Basic Constraints  | CA = FALSE                          |
| SAN                | IP Address: 192.168.80.100          |

**Q6. What is the difference between the server certificate and the chain file in Apache config?**

| Directive               | File          | Contains                     |
| ----------------------- | ------------- | ---------------------------- |
| SSLCertificateFile      | webserver.crt | Server's own certificate     |
| SSLCertificateKeyFile   | webserver.key | Server's private key         |
| SSLCertificateChainFile | chain.crt     | Sub CA + Root CA (for trust) |
