insecure ipc - inter process communication

activity A -> activity B -> activity C

attacker activity A -> activity C

sysntex: am start -n package_name/activity_name
$ adb shell su -c 'am start -n '

# 1. Launcher (works)
adb shell su -c "am start -n com.poondyapp.android/com.dinosoftlabs.binder.Splash_A"

# 2. Login_A (fails – not exported)
adb shell su -c "am start -n com.poondyapp.android/com.dinosoftlabs.binder.Accounts.Login_A"

# 3. Enable_location_A (fails)
adb shell su -c "am start -n com.poondyapp.android/com.dinosoftlabs.binder.Accounts.Enable_location_A"

# 4. Login_Phone_A (fails)
adb shell su -c "am start -n com.poondyapp.android/com.dinosoftlabs.binder.Accounts.Login_Phone_A"

# 5. Get_User_Info_A (fails)
adb shell su -c "am start -n com.poondyapp.android/com.dinosoftlabs.binder.Accounts.Get_User_Info_A"

# 6. MainMenuActivity (fails)
adb shell su -c "am start -n com.poondyapp.android/com.dinosoftlabs.binder.Main_Menu.MainMenuActivity"

# 7. MapsActivity (fails)
adb shell su -c "am start -n com.poondyapp.android/com.dinosoftlabs.binder.GoogleMap.MapsActivity"

# 8. SearchPlaces (fails)
adb shell su -c "am start -n com.poondyapp.android/com.dinosoftlabs.binder.GoogleMap.SearchPlaces"


insecure in the application 
````````````````````````````

adb logcat (to monitert debuglog for the entire devide )


pidcat   app package name moniter the individula application logs 


ex pid com.campus360


`````````````````
mobSF
`````````````````

hybride mobile application scanning 

android ios etc


for static and dynamic (sast ,dast for mobile application)


docker pull opensecurity/mobile-security-framework-mobsf:latest
docker run -it --rm -p 8000:8000 opensecurity/mobile-security-framework-mobsf:latest

# Default username and password: mobsf/mobsf




it will take 5 - 10 min , to install the dependencies 
so, wait

